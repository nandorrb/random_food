# Config settings
title: mecatronica.pe
baseurl: "/random_food"

# Random redirect settings
random_redirect:
  pages: true

# Build settings
markdown: kramdown
theme: minima
gems:
  - jekyll-feed
exclude:
  - Gemfile
  - Gemfile.lock
